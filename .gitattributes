# Auto detect text files and perform LF normalization
* text=auto

# index.html is generated from src/ by build.py. Keep it in the repo — a fresh
# clone must contain a runnable deck — but keep it out of diffs and out of the
# language stats, so review shows the source change and not the rebuild.
index.html -diff linguist-generated=true
